openapi: 3.1.0
info:
  title: Mini Life Service
  version: 0.0.0
tags:
  - name: Health
paths:
  /health:
    get:
      operationId: health
      parameters: []
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                required:
                  - success
      tags:
        - Health
components: {}
